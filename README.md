## Steps to run project locally
1. Clone the project
2. Get `apikey` from `omdbapi.com`
3. Paste `apikey` in `src/app/service/movie-service.service.ts`
4. Hit `npm start` from terminal inside the project directory
5. Browsw at `http://localhost:4200`
